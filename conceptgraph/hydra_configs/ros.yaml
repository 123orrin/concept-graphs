defaults:
  - base_paths

# Not used (just here to prevent errors)
dataset_root: ${data_root}/external
#scene_id: conceptgraphs_short
scene_id: tablewares #modify for tablewares, but will be covered by settings in ros_tableware.yaml
dataset_config: ${repo_root}/conceptgraph/dataset/dataconfigs/lsy.yaml
# Not used (end)

color_path: ${dataset_root}/${scene_id}/color #
depth_path: ${dataset_root}/${scene_id}/depth
pose_path: ${dataset_root}/${scene_id}/pose
intrinsics_path: ${dataset_root}/${scene_id}/intrinsics

save_color: True
save_depth: False
save_pose: True
save_intrinsics: True
#device: "cpu"

rotate: False # cfg.rotate set false

use_pc_for_depth: True

min_depth: 0.1
max_depth: 10.0

true_height: 720
true_width: 1280

desired_height: 370 # commented out for T2
desired_width: 640 # commented out for T2
# desired_height: 240 #for T2
# desired_width: 320 #for T2

periodically_save_pcd: True 
periodically_save_pcd_interval: 10

camera_params:
  # image_height: 720
  # image_width: 1280
  # fx: 762.72
  # fy: 754.75
  # cx: 640.0
  # cy: 360.0
  # image_height: 704
  # image_width: 1260
  # fx: 762.72
  # fy: 754.75
  # cx: 627.0
  # cy: 360.0
  image_height: 720
  image_width: 1280
  # fx: 750.025
  # fy: 749.733
  # cx: 636.264
  # cy: 369.197
  fx: 791.7353879123823
  fy: 791.7353879123823
  cx: 640.0
  cy: 360.0
  png_depth_scale: 1000 #for depth image in png format
  crop_edge: 0



